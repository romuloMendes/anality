; php.ini local do projeto — usado pelo artisan serve via PHPRC=.
; Referenciado pelo script "dev" do composer.json com PHPRC=$(pwd)

upload_max_filesize = 100M
post_max_size       = 120M
max_input_time      = 300
max_execution_time  = 300
memory_limit        = 512M
